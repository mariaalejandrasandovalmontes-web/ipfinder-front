import React from "react";
import ReactDOM from "react-dom/client";
import FinderApp from "./FinderApp"; // importa tu componente

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <FinderApp />
    </React.StrictMode>
);